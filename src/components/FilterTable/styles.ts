import { createStyles } from 'antd-style';
 
export const useStyles = createStyles(({ css, cx }) => ({
  
  table: css`
    position: relative;
    top:400px;
    left:100px;
    width: 180px;
    
  `,
}));